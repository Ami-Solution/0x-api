import { Request, Response } from 'express';
import TokenPairs = require('../models/tokenPairsModel');

export class TokenController {

  public getTokenPairs(req: Request, res: Response) {
    // Return list of token pairs
    
  }
}
